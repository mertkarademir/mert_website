{
  "name": "photo-website",
  "version": "1.0.0",
  "description": "A single-page app for uploading and viewing plant and mushroom photos with categories and captions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "npm install && (cd client && npm install && npm run build) || true",
    "dev:full": "concurrently \"npm run dev\" \"npm run client\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "concurrently": "^6.0.0",
    "nodemon": "^2.0.7"
  }
}
